\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig13_latency_load.png}
  \caption{Fig. 13. Mean end-to-end pipeline latency (ms) as a function of concurrent email request load for GPT-4-turbo, GPT-3.5-turbo, and Mistral-7B-Instruct backends. Mistral-7B meets the 2-second SLA up to 50 concurrent requests; GPT-3.5-turbo satisfies the SLA up to 30 requests.}
  \label{fig:fig13_latency_load}
\end{figure}